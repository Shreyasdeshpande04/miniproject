<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>HomePage</title>

  <!-- Bootstrap + Font Awesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet"/>

  <style>
    body {
      background-color: #ffffff;
      font-family: Arial, sans-serif;
    }

    .section-title {
      text-align: center;
      font-weight: bold;
      color: #333;
      padding: 10px;
    }

    .section-row {
  display: flex;
  flex-direction: column; /* Force vertical stacking */
  gap: 30px;
  padding: 20px;
}


    .section-container {
      flex: 0 0 33%;
      min-width: 400px;
      background-color: #FFE8CD;
      border-radius: 10px;
      padding: 15px;
      box-shadow: 0 0 8px rgba(0,0,0,0.2);
      position: relative;
    }

    .section-box {
      border: 2px solid #ccc;
      border-radius: 10px;
      padding: 10px;
      height: 100%;
    }

    .post-count {
      position: absolute;
      top: 20px;
      right: 20px;
      background-color: #222;
      color: white;
      padding: 5px 12px;
      font-size: 13px;
      border-radius: 6px;
    }

    .scroll-wrapper {
      display: flex;
      overflow-x: auto;
      scroll-behavior: smooth;
      padding: 10px 0;
    }

    .scroll-card {
      flex: 0 0 auto;
      width: 330px;
      margin-right: 15px;
    }

  .card {
  height: 100%;
  max-height: 300px;         /* NEW: Fixed height */
  overflow-y: auto;          /* NEW: Vertical scroll if needed */
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  border-radius: 8px;
  border: none;
  box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
  padding: 10px;
  background-color: #FFD6BA;
  transition: transform 0.2s;
}

    .card:hover {
      transform: scale(1.02);
    }

    table {
      width: 100%;
      font-size: 14px;
      background: transparent;
    }

    th, td {
      padding: 8px;
      border: 1px solid #000000;
      vertical-align: top;
    }

    th {
      background-color: #FFD6BA;
    }

    .btn-custom {
      font-size: 13px;
      padding: 5px 10px;
    }

    /* Responsive Mobile View */
    @media (max-width: 768px) {
      .section-row {
        flex-direction: column;
        padding: 10px;
      }

      .section-container {
        width: 100%;
        min-width: 100%;
        margin-bottom: 30px;
      }

      .scroll-wrapper {
        flex-direction: column;
        overflow-x: unset;
        overflow-y: auto;
        max-height: 600px;
      }

      .scroll-card {
        width: 100%;
        margin-bottom: 20px;
      }
    }
    .fas fa-share{
      color: blue;
    }
  </style>
</head>
<body>

  <!-- Header Placeholder -->
  <%- include('header', { loggedIn: loggedIn }) %>

  <div class="container-fluid">
    <div class="section-row">

      <!-- MongoDB Section -->
      <div class="section-container">
        <h2 class="section-title">🚀 MONGO DB POSTS 🚀</h2>
        <div class="post-count">Total Posts: <%= users.filter(u => u.course === 'Mongodb').length %></div>
        <div class="section-box">
          <div class="scroll-wrapper">
            <% let mongoIndex = 0; %>
            <% users.filter(u => u.course === 'Mongodb').forEach((user, i) => { %>
              <div class="scroll-card">
                <div class="card">
                  <table>
                    <tr><th>Day</th><td><%= ++mongoIndex %></td></tr>
                    <tr><th>Topic</th><td><%= user.topic %></td></tr>
                    <tr><th>Description</th><td><%= user.description %></td></tr>
                    <tr><th>LinkedIn</th>
                      <td>
                        <a href="<%= user.llink %>" target="_blank" class="btn btn-primary btn-sm btn-custom"><i class="fab fa-linkedin"></i> Visit</a>
                        <button class="btn btn-light btn-sm btn-custom" onclick="copyToClipboard('<%= user.llink %>')"><i class="fas fa-share"></i> Share</button>
                      </td>
                    </tr>
                    <tr><th>GitHub</th>
                      <td>
                        <a href="<%= user.glink %>" target="_blank" class="btn btn-dark btn-sm btn-custom"><i class="fab fa-github"></i> Visit</a>
                        <button class="btn btn-light btn-sm btn-custom" onclick="copyToClipboard('<%= user.glink %>')"><i class="fas fa-share"></i> Share</button>
                      </td>
                    </tr>
                  </table>
                </div>
              </div>
            <% }) %>
          </div>
        </div>
      </div>

      <!-- NodeJS Section -->
      <div class="section-container">
        <h2 class="section-title">🚀 NODE JS POSTS 🚀</h2>
        <div class="post-count">Total Posts: <%= users.filter(u => u.course === 'Nodejs').length %></div>
        <div class="section-box">
          <div class="scroll-wrapper">
            <% let nodeIndex = 0; %>
            <% users.filter(u => u.course === 'Nodejs').forEach((user, i) => { %>
              <div class="scroll-card">
                <div class="card">
                  <table>
                    <tr><th>Day</th><td><%= ++nodeIndex %></td></tr>
                    <tr><th>Topic</th><td><%= user.topic %></td></tr>
                    <tr><th>Description</th><td><%= user.description %></td></tr>
                    <tr><th>LinkedIn</th>
                      <td>
                        <a href="<%= user.llink %>" target="_blank" class="btn btn-primary btn-sm btn-custom"><i class="fab fa-linkedin"></i> Visit</a>
                        <button class="btn btn-light btn-sm btn-custom" onclick="copyToClipboard('<%= user.llink %>')"><i class="fas fa-share"></i> Share</button>
                      </td>
                    </tr>
                    <tr><th>GitHub</th>
                      <td>
                        <a href="<%= user.glink %>" target="_blank" class="btn btn-dark btn-sm btn-custom"><i class="fab fa-github"></i> Visit</a>
                        <button class="btn btn-light btn-sm btn-custom" onclick="copyToClipboard('<%= user.glink %>')"><i class="fas fa-share"></i> Share</button>
                      </td>
                    </tr>
                  </table>
                </div>
              </div>
            <% }) %>
          </div>
        </div>
      </div>

      <!-- ExpressJS Section -->
      <div class="section-container">
        <h2 class="section-title">🚀 EXPRESS JS POSTS 🚀</h2>
        <div class="post-count">Total Posts: <%= users.filter(u => u.course === 'Expressjs').length %></div>
        <div class="section-box">
          <div class="scroll-wrapper">
            <% let expressIndex = 0; %>
            <% users.filter(u => u.course === 'Expressjs').forEach((user, i) => { %>
              <div class="scroll-card">
                <div class="card">
                  <table>
                    <tr><th>Day</th><td><%= ++expressIndex %></td></tr>
                    <tr><th>Topic</th><td><%= user.topic %></td></tr>
                    <tr><th>Description</th><td><%= user.description %></td></tr>
                    <tr><th>LinkedIn</th>
                      <td>
                        <a href="<%= user.llink %>" target="_blank" class="btn btn-primary btn-sm btn-custom"><i class="fab fa-linkedin"></i> Visit</a>
                        <button class="btn btn-light btn-sm btn-custom" onclick="copyToClipboard('<%= user.llink %>')"><i class="fas fa-share"></i> Share</button>
                      </td>
                    </tr>
                    <tr><th>GitHub</th>
                      <td>
                        <a href="<%= user.glink %>" target="_blank" class="btn btn-dark btn-sm btn-custom"><i class="fab fa-github"></i> Visit</a>
                        <button class="btn btn-light btn-sm btn-custom" onclick="copyToClipboard('<%= user.glink %>')"><i class="fas fa-share"></i> Share</button>
                      </td>
                    </tr>
                  </table>
                </div>
              </div>
            <% }) %>
          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Footer Placeholder -->
  <%- include('footer') %>

  <script>
    function copyToClipboard(link) {
      navigator.clipboard.writeText(link)
        .then(() => alert("🔗 Link copied to clipboard!"))
        .catch(err => alert("Failed to copy: " + err));
    }
  </script>

</body>
</html>
