<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Table</title>
  <style>
    body {
      font-family: sans-serif;
    }

    .styled-table {
      border-collapse: collapse;
      margin: 25px auto;
      font-size: 0.9em;
      min-width: 900px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);
    }

    .styled-table thead tr {
      background-color: #FFDCDC;
      color: #000000;
      text-align: left;
    }

    .styled-table th, .styled-table td {
      padding: 12px 15px;
      border: 1px solid black;
    }

.styled-table tbody tr {
  background-color: #f5e6cc; /* Light brown */
}

    h1 {
      text-align: center;
      margin-top: 30px;
    }

    button {
      margin: 2px;
      padding: 5px 10px;
      border: none;
      background-color: #009879;
      color: white;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      background-color: #007e6c;
    }

    form {
      display: inline;
    }
  </style>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body style="background-color:#FFF2EB"">

  <%- include('header') %>
  <h1>WELCOME TO POST TABLE PAGE</h1>

  <table class="styled-table" border="3">
    <thead>
      <tr>
        <th>SR NO</th>
        <th>COURSE</th>
        <th>TOPIC</th>
        <th>DESCRIPTION</th>
        <th>LINKEDIN LINK</th>
          <th>GITHUB LINK</th>
        <th>ACTIONS</th>
      </tr>
    </thead>
    <tbody>
      <% for (var i = 0; i < users.length; i++) { %>
        <tr>
          <td><%= i + 1 %></td>
          <td><%= users[i].course %></td>
          <td><%= users[i].topic %></td>
          <td><%= users[i].description %></td>
          <td>
            <a href="<%= users[i].llink %>" target="_blank"><%= users[i].llink %></a>
          </td>
             <td>
            <a href="<%= users[i].glink %>" target="_blank"><%= users[i].glink %></a>
          </td>
          <td>
            <a href="/edit/<%= users[i]._id %>" onclick="return confirm('Are you sure you want to edit this field?');">
              <button type="button">EDIT</button>
            </a><br><br>
  <form action="/delete/<%= users[i]._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this field?');">
  <button type="submit" class="btn btn-danger btn-sm">DELETE</button>
</form>

              
         
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>

  <%- include('footer') %>

</body>
</html>
